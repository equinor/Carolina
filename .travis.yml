dist: trusty
language: python
python:
  - "3.6"

addons:
  apt:
    sources:
      - sourceline: 'ppa:jokva/carolina'
    packages:
      - dakota1
      - libdakota-dev
      - libboost-python-dev
      - libboost-all-dev

script:
  - ./setup.py build
  - export PYTHONPATH=$PYTHONPATH:$PWD/build/lib.linux-x86_64-3.6
  - ./setup.py test
